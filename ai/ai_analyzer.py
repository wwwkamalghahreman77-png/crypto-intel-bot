"""
ai/ai_analyzer.py

# TODO (توسعه آینده):
این ماژول محل آماده برای اضافه‌کردن تحلیل مبتنی بر AI است، مثلاً:
    - خلاصه‌سازی خودکار اخبار با یک مدل زبانی
    - تحلیل احساسات (Sentiment Analysis) دقیق‌تر برای توییتر/تلگرام
    - تشخیص الگوهای نموداری با مدل‌های یادگیری ماشین

در نسخه فعلی (MVP رایگان)، این ماژول فقط یک تابع نمونه دارد که در صورت
تنظیم‌نشدن کلید API، به‌صورت شفاف اعلام می‌کند که این قابلیت غیرفعال است.
هیچ کلید API ساختگی یا داده جعلی در این فایل استفاده نشده است.
"""

import os

AI_API_KEY = os.getenv("AI_API_KEY", "")  # در صورت تمایل به فعال‌سازی، در .env اضافه شود


def ai_summarize(text: str) -> str:
    if not AI_API_KEY:
        return "[AI غیرفعال است - برای فعال‌سازی، AI_API_KEY را در .env تنظیم کنید]"

    # نمونه پیاده‌سازی با Anthropic API (نیازمند کتابخانه anthropic و کلید معتبر)
    # این بخش عمداً پیاده‌سازی کامل نشده تا پروژه بدون کلید AI هم اجرا شود.
    raise NotImplementedError("پیاده‌سازی اتصال AI هنوز تکمیل نشده است.")
  
